{
  "description": "Architect Agent hooks",
  "_note": "Hook data is passed via stdin as JSON. Scripts read from stdin, not env vars.",
  "hooks": {
    "Stop": [
      {
        "_id": "eaa-stop-check",
        "_description": "Blocks exit until all design work is complete",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/eaa_stop_check.py"
          }
        ]
      }
    ]
  }
}
