{
  "description": "Architect Agent hooks - skill activation for design workflows",
  "_note": "Hook data is passed via stdin as JSON. Scripts read from stdin, not env vars.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "_id": "arch-skill-activation",
        "_description": "Activate relevant design and planning skills based on user prompt",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/arch_skill_activation.py"
          }
        ]
      }
    ]
  }
}
